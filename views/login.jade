.row
  .span10
    - if ('undefined' !== typeof errors && errors.length)
      ul#errors
        - each error in errors
          li.error= error
.row-fluid
  .span2
  .span6
    .hero-unit
      h1 Brilliant Continuous Deployment.
      br
      p Strider is a hosted continuous delivery platform for Python and Node.js. 
      p Strider is curently in private beta.
  .span3
    .row-fluid.hero-subunit
      h3 Existing User?
      br
      br
      form(action='/login', method='post')
        div.clearfix
          label(for=everyauth.password.loginFormFieldName) Email
          div.input
            input.xlarge(type='text', name=everyauth.password.loginFormFieldName, value=email)
        div.clearfix
          label(for=everyauth.password.passwordFormFieldName) Password
          div.input
            input.xlarge(type='password', name=everyauth.password.passwordFormFieldName)
        div.actions
          input.btn.primary(type='submit', value='login')
      br
      br
      br
    .row-fluid.hero-subunit
      form.validate(action="https://docs.google.com/a/beyondfog.com/spreadsheet/formResponse?formkey=dGo4TkN0ZlhTSHdzbzBjRHNoTHltTHc6MQ&ifq", method="post")
        h3 Request invite:
        br
        input.email(type='email', placeholder="Email Address", name="entry.0.single")
        div.actions
          input.btn.primary(type="submit", value="request code", name="submit")
          input(type='hidden', name='pageNumber', value='0')
          input(type='hidden', name='backupCache', value='')


script
  $(document).ready(function() {
    $('#layout-header').hide();
    $('#navbar-signin-form').hide();

  });
