script.template#github-collaborators-template(type="text/template")
  div#github-collaborators.span8
    .well
      fieldset
        legend Add Collaborators?
        div.alert.alert-success.hide
        p Collaborators will be notified of build success/failure, be able to trigger test jobs and deploys, and access job details. They will not be permitted to add other new collaborators nor change any project configuration. You can add more at any time on the project configuration page.
        .row
          .span3
            .control-group
              .controls
                .input-append
                  input.collab-email.span2(type='text', placeholder="email address")
                  button.btn.btn-add-collab Add

script.template#github-setup(type="text/template")
  div.span8
    div.github-main-content
      p You have selected the following Github repository:

      p
      |  <b><em><%= display_name %></em></b>
      |  <% if (private) { %>
      |    <p><i class="icon-lock"></i> This repository is private.</p>
      |  <% } %>


      p
      | Strider needs a Github post-receive hook and a deploy key 
      | in order to run tests against <strong><%= display_name %></strong> 
      | after each Github check-in. Strider can setup both the post-receive 
      | hook and the deploy key automatically. Please click below to 
      | authorize strider to configure <strong><%= display_name %></strong> 
      | on your behalf.

    p
    div.actions
      a.run-action.btn.btn-primary Yes, please configure my project
